# Archived workflows

These files preserve inherited maintainer and publishing automation without
registering it with GitHub Actions.

Do not move a workflow back into `.github/workflows/` until its triggers,
permissions, credentials, artifact provenance, and Nagi release boundary have
been reviewed end to end. Nagi currently ships from source only.
